/**
 * Funcion que nos devuelve la potencia de un numero.
 * @constructor
 * @param {Int} numbase - El numero base el cual se va a exponenciar.
 * @param {Int} numexp - El numero exponenciador.
 */


export function potencia(numbase,numexp){
    return numbase ^ numexp;
}